package view

// Hello renders the initial page inside the shared layout.
templ Hello(stylesheetURL string) {
	@layout(stylesheetURL) {
		<main class="p-8">
			<h1 class="text-2xl">Hello from ribbitto</h1>
		</main>
	}
}
